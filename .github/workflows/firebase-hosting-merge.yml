# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge

on:
  push:
    branches:
      - main

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Install dependencies
        run: npm ci

      - name: Build project with secrets
        env:
          VITE_API_KEY: ${{ 'AIzaSyBk9haiRs4KbK5dCZPYB51xvJAtgdQ1N5s' }}
          VITE_AUTH_DOMAIN: ${{ 'notes-app-vn01.firebaseapp.com' }}
          VITE_PROJECT_ID: ${{ 'notes-app-vn01' }}
          VITE_STORAGE_BUCKET: ${{ 'notes-app-vn01.firebasestorage.app' }}
          VITE_MESSAGING_SENDER_ID: ${{ '570934402735' }}
          VITE_APP_ID: ${{ '1:570934402735:web:e4e5d75c78be5e78493e5a' }}
        run: npm run build

      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_NOTES_APP_VN01 }}
          channelId: live
          projectId: notes-app-vn01

